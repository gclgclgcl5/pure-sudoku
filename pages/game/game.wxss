/* 游戏页面样式 - 宁静青蓝主题 */
@import '../../styles/pet-dock-shared.wxss';

.container {
  min-height: 100vh;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  background: linear-gradient(180deg, #F8FAFB 0%, #EDF5F5 100%);
}

.content {
  flex: 1;
  display: flex;
  flex-direction: column;
  padding: 20rpx 30rpx;
  padding-bottom: 20rpx; /* 减少底部留白 */
}

/* 顶部信息栏 - 更宽，字体更大 */
.info-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx 20rpx;
  background: linear-gradient(135deg, #5EAAA8, #4A8886);
  border-radius: 12rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 12rpx rgba(94, 170, 168, 0.25);
  gap: 10rpx;
}

/* 信息项（通用，各占1/3） */
.info-item {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
  gap: 6rpx;
  flex: 1;
  min-width: 0;
}

.info-label {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.9);
  font-weight: 500;
  line-height: 1;
}

/* 难度显示（横向布局） */
.difficulty-item {
  flex-direction: row;
}

.difficulty-item .info-value {
  font-size: 26rpx;
  color: #ffffff;
  font-weight: bold;
  line-height: 1;
}

/* 错误计数 */
.error-item {
  gap: 6rpx;
}

.error-count {
  font-size: 26rpx;
  color: #ffffff;
  font-weight: bold;
  line-height: 1;
}

/* 计时器 */
.timer-item {
  gap: 6rpx;
}

.timer-text {
  font-size: 26rpx;
  color: #ffffff;
  font-weight: bold;
  font-family: 'Courier New', monospace;
  line-height: 1;
  text-align: center;
}

/* 最佳时间 - 更紧凑 */
.best-time {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 8rpx;
  padding: 8rpx 16rpx;
  background: #D4EDEC;
  border-radius: 8rpx;
  margin-bottom: 20rpx;
}

.best-time-label {
  font-size: 22rpx;
  color: #6B9A99;
  font-weight: 500;
}

.best-time-value {
  font-size: 24rpx;
  color: #2C5F5D;
  font-weight: bold;
  font-family: 'Courier New', monospace;
}

/* 移除旧的新游戏按钮样式（已移到信息栏内） */

/* 广告位已移除，释放空间给数字按钮 */

/* 数独棋盘 - 使用固定尺寸 */
.board {
  display: grid;
  grid-template-columns: repeat(9, 73rpx);  /* 直接指定每列宽度 */
  gap: 2rpx;  /* 格子之间的间距 */
  background-color: #2d3748;
  padding: 4rpx;  /* 外边距 */
  border-radius: 8rpx;
  margin: 0 auto 16rpx auto;  /* 增加底部间距 */
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.12);
  position: relative;
  transition: all 0.3s ease;
}

/* 暂停状态的棋盘 - 移除模糊效果 */
.board.paused {
  /* 不需要模糊效果 */
}

/* 暂停状态下隐藏3×3粗线 */
.board.paused .cell.border-right::after,
.board.paused .cell.border-bottom::before {
  display: none;
}

/* 单元格 - 宁静青蓝主题 */
.cell {
  width: 73rpx;
  height: 73rpx;
  background-color: #FFFFFF;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.15s ease;
  position: relative;
}

.cell-text {
  font-size: 36rpx;
  color: #2C5F5D;
  font-weight: 500;
  transition: all 0.15s ease;
}

/* 固定数字（初始题目） - 保持浅色 */
.cell.fixed {
  background-color: #F0F9F8;
}

.cell.fixed .cell-text {
  color: #2C5F5D;
  font-weight: 700;
}

/* 选中状态 - 深青蓝，更明显 */
.cell.selected {
  background-color: #7FCCC9 !important;
  box-shadow: inset 0 0 0 5rpx #4A8886;
  z-index: 10;
}

.cell.selected .cell-text {
  color: #FFFFFF;
  font-weight: 700;
  font-size: 42rpx;
  text-shadow: 0 1rpx 2rpx rgba(0, 0, 0, 0.1);
}

/* 相关区域（同行/同列/同宫格） - 明显的青色 */
.cell.related {
  background-color: #C8E8E7;
}

/* 同数字高亮：背景与描边由 game.wxml 内联 theme.colors.sameDigitHighlight / primaryDark */
.cell.same-number {
  z-index: 1;
}

.cell.same-number .cell-text {
  font-weight: 700;
  font-size: 38rpx;
}

/* 错误抖动动画 */
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-8rpx); }
  75% { transform: translateX(8rpx); }
}

.cell.error {
  animation: shake 0.4s ease;
}

/* 3x3宫格边框 - 使用伪元素确保不被其他样式覆盖 */
.cell.border-right::after {
  content: '';
  position: absolute;
  right: 0;
  top: 0;
  bottom: 0;
  width: 4rpx;
  background-color: #1a202c;
  pointer-events: none;
  z-index: 100;
}

.cell.border-bottom::before {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 4rpx;
  background-color: #1a202c;
  pointer-events: none;
  z-index: 100;
}

/* 优先级：提示 > 错误 > 相同数字 > 选中 > 相关 */
.cell.error {
  background-color: #FFF0F2 !important;
  box-shadow: inset 0 0 0 3rpx #E89BA3 !important;
}

.cell.error .cell-text {
  color: #E89BA3;
  font-weight: 700;
}

/* 提示状态 - 绿色高亮 */
.cell.hint-active {
  background: linear-gradient(135deg, #48bb78, #38a169) !important;
  box-shadow: 0 0 20rpx rgba(72, 187, 120, 0.6), inset 0 0 0 4rpx #2f855a !important;
  animation: hint-pulse 1.5s ease-in-out infinite;
  z-index: 20;
}

/* 提示数字样式 */
.hint-text {
  font-size: 48rpx;
  color: #ffffff;
  font-weight: 900;
  text-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.3);
  animation: hint-appear 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
}

/* 笔记模式：候选数字网格（3×3布局） */
.notes-grid {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-template-rows: repeat(3, 1fr);
  gap: 0;
  padding: 2rpx;
}

.note-cell {
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18rpx;
  font-weight: 600;
  line-height: 1;
}

.note-cell.show {
  color: #5EAAA8;
  opacity: 1;
}

.note-cell.hide {
  color: transparent;
  opacity: 0;
}

/* 提示出现动画 */
@keyframes hint-appear {
  0% {
    transform: scale(0) rotate(-180deg);
    opacity: 0;
  }
  50% {
    transform: scale(1.2) rotate(0deg);
  }
  100% {
    transform: scale(1);
    opacity: 1;
  }
}

/* 提示脉冲动画 */
@keyframes hint-pulse {
  0%, 100% {
    transform: scale(1);
    box-shadow: 0 0 20rpx rgba(72, 187, 120, 0.6), inset 0 0 0 4rpx #2f855a;
  }
  50% {
    transform: scale(1.05);
    box-shadow: 0 0 30rpx rgba(72, 187, 120, 0.9), inset 0 0 0 4rpx #2f855a;
  }
}

/* 暂停遮罩 */
.pause-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(255, 255, 255, 0.95);
  border-radius: 8rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 30;
  animation: fade-in 0.3s ease;
}

.pause-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
}

.pause-icon {
  font-size: 80rpx;
  color: #5EAAA8;
  animation: pause-bounce 2s ease-in-out infinite;
}

.pause-text {
  font-size: 36rpx;
  font-weight: bold;
  color: #2C5F5D;
}

.pause-hint {
  font-size: 24rpx;
  color: #6B9A99;
  margin-bottom: 20rpx;
}

/* 暂停界面操作按钮 */
.pause-actions {
  display: flex;
  gap: 16rpx;
  margin-top: 20rpx;
}

.pause-action-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  background: linear-gradient(135deg, #5EAAA8, #4A8886);
  color: #FFFFFF;
  border-radius: 12rpx;
  padding: 20rpx 32rpx;
  min-width: 160rpx;
  box-shadow: 0 4rpx 12rpx rgba(94, 170, 168, 0.3);
  transition: all 0.2s;
}

.pause-action-btn:active {
  transform: scale(0.95);
  box-shadow: 0 2rpx 8rpx rgba(94, 170, 168, 0.4);
}

.pause-action-icon {
  font-size: 40rpx;
  line-height: 1;
}

.pause-action-label {
  font-size: 26rpx;
  font-weight: 600;
  line-height: 1;
}

/* 暂停图标弹跳动画 */
@keyframes pause-bounce {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-10rpx);
  }
}

/* 淡入动画 */
@keyframes fade-in {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

/* 底部工具栏：外层仅负责全宽对齐；白底在 .bottom-toolbar-keys，宠物在 .pet-dock-strip */
.bottom-toolbar {
  background: transparent;
  margin: 16rpx -30rpx 0 -30rpx; /* 负 margin 与内容区等宽 */
  padding: 0;
  box-shadow: none;
}

.bottom-toolbar-keys {
  padding: 12rpx 20rpx;
  box-sizing: border-box;
}

/* 宠物条：主题浅底，与上方数字键区域区分（背景色由 WXML 内联 theme.colors.background） */
.pet-dock-strip {
  padding: 12rpx 20rpx calc(16rpx + env(safe-area-inset-bottom));
  box-sizing: border-box;
}

/* 功能按钮行 - 与下方数字键同高，视觉统一 */
.toolbar-actions {
  display: flex;
  justify-content: space-between;
  align-items: stretch;
  gap: 5rpx;
  margin-bottom: 10rpx;
  padding: 0 5rpx;
}

.tool-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  flex: 1;
  min-width: 0;
  height: 100rpx;
  min-height: 100rpx;
  max-height: 100rpx;
  box-sizing: border-box;
  gap: 2rpx;
  background: #f7fafc;
  border: 2rpx solid #e2e8f0;
  border-radius: 12rpx;
  padding: 4rpx 6rpx;
  margin: 0;
  line-height: 1.1;
  font-size: 20rpx;
  transition: all 0.2s;
}

.bottom-toolbar.number-pad-double .tool-btn {
  height: 108rpx;
  min-height: 108rpx;
  max-height: 108rpx;
}

.tool-btn:active {
  transform: scale(0.95);
  background: #edf2f7;
}

.tool-btn.active {
  background: linear-gradient(135deg, #5EAAA8, #4A8886);
  border-color: #5EAAA8;
  box-shadow: 0 0 16rpx rgba(94, 170, 168, 0.4);
  animation: tool-active-pulse 2s ease-in-out infinite;
}

.tool-btn.active .tool-icon,
.tool-btn.active .tool-label {
  color: #FFFFFF;
}

@keyframes tool-active-pulse {
  0%, 100% {
    box-shadow: 0 0 16rpx rgba(66, 153, 225, 0.5);
  }
  50% {
    box-shadow: 0 0 24rpx rgba(66, 153, 225, 0.8);
  }
}

.tool-btn::after {
  border: none;
}

.tool-btn:disabled {
  opacity: 0.4;
  transform: none;
}

.tool-icon {
  font-size: 26rpx;
  color: #5EAAA8;
  line-height: 1;
}

.tool-label {
  font-size: 18rpx;
  color: #2C5F5D;
  font-weight: 500;
  line-height: 1;
}

/* 数字网格 - 更紧凑 */
.number-grid {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
  margin-bottom: 10rpx;
}

.number-row {
  display: grid;
  gap: 5rpx;
}

.number-grid.one-row .number-row {
  grid-template-columns: repeat(9, 1fr);
}

.number-grid.two-rows .number-row:first-child {
  grid-template-columns: repeat(5, 1fr);
  /* 固定行高，避免 Grid 行被内容撑得比第二行更高 */
  grid-auto-rows: 108rpx;
  align-items: stretch;
}

/* 双行第二行：与第一行单格同宽，整体居中（不铺满整行） */
.number-grid.two-rows .number-row:last-child {
  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
  gap: 5rpx;
  height: 108rpx;
  min-height: 108rpx;
  max-height: 108rpx;
  box-sizing: border-box;
}

.number-grid.two-rows .number-row:last-child .number-btn {
  flex-grow: 0;
  flex-shrink: 0;
  flex-basis: calc((100% - 20rpx) / 5);
  width: calc((100% - 20rpx) / 5);
  max-width: calc((100% - 20rpx) / 5);
  height: 108rpx;
  min-height: 108rpx;
  max-height: 108rpx;
  box-sizing: border-box;
}

/* 数字按钮 - 增大尺寸，显示剩余数量 */
.number-btn {
  height: 100rpx;
  min-height: 100rpx;
  max-height: 100rpx;
  box-sizing: border-box;
  background: #f7fafc;
  border-radius: 12rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 4rpx;
  transition: all 0.2s;
  border: 2rpx solid #e2e8f0;
  padding: 8rpx 0;
}

.number-grid.two-rows .number-btn {
  height: 108rpx;
  min-height: 108rpx;
  max-height: 108rpx;
}

.number-main {
  font-size: 48rpx;
  font-weight: bold;
  color: #2C5F5D;
  line-height: 1;
}

.number-count {
  font-size: 20rpx;
  color: #9BBCBB;
  font-weight: 500;
  line-height: 1;
}

.number-btn:active {
  background: #B4E4E3;
  transform: scale(0.92);
  border-color: #5EAAA8;
}

.number-btn:active .number-main {
  color: #2C5F5D;
}

.number-btn:active .number-count {
  color: #5EAAA8;
}

/* 笔记模式样式 - 宁静青蓝 */
.number-btn.note-mode {
  background: linear-gradient(135deg, #D4EDEC, #B4E4E3);
  border: 3rpx solid #7FCCC9;
  box-shadow: 0 0 12rpx rgba(127, 204, 201, 0.3), inset 0 0 8rpx rgba(127, 204, 201, 0.1);
  animation: note-mode-pulse 1.5s ease-in-out infinite;
}

.number-btn.note-mode .number-main {
  color: #5EAAA8;
}

.number-btn.note-mode .number-count {
  color: #7FCCC9;
}

.number-btn.note-mode:active {
  background: #5EAAA8;
  transform: scale(0.92);
}

.number-btn.note-mode:active .number-main {
  color: #FFFFFF;
}

.number-btn.note-mode:active .number-count {
  color: #FFFFFF;
}

@keyframes note-mode-pulse {
  0%, 100% {
    box-shadow: 0 0 12rpx rgba(243, 156, 18, 0.4), inset 0 0 8rpx rgba(243, 156, 18, 0.1);
  }
  50% {
    box-shadow: 0 0 20rpx rgba(243, 156, 18, 0.6), inset 0 0 12rpx rgba(243, 156, 18, 0.2);
  }
}

/* 操作按钮 */
/* 移除提示信息（不需要了） */

/* ========== 宠物 dock ========== */
.pet-dock {
  margin-top: 0;
  padding: 4rpx 0 0;
  width: 100%;
  box-sizing: border-box;
}

/* 双宠：左右固定，各半宽气泡不顶到中间 */
.pet-dock-dual-row {
  display: flex;
  flex-direction: row;
  align-items: flex-start;
  width: 100%;
  box-sizing: border-box;
  gap: 8rpx;
}

.pet-dock-half {
  flex: 1;
  width: 0;
  min-width: 0;
  box-sizing: border-box;
}

.pet-dock-half .pet-dock-inner {
  width: 100%;
}

.pet-dock-half .pet-bubble-wrap {
  max-width: 100%;
}

.pet-dock-half .pet-bubble {
  max-width: 100%;
}

/* 游戏内宠物气泡：淡入淡出（由 petBubbleFadeIn / petBubble1FadeIn 驱动） */
.pet-bubble.pet-bubble--gamefx {
  opacity: 0;
  transform: translateY(10rpx);
  transition: opacity 0.28s ease, transform 0.28s ease;
}

.pet-bubble.pet-bubble--gamefx.is-visible {
  opacity: 1;
  transform: translateY(0);
}

/* ========== 通关宠物蒙层 ========== */
.win-pet-mask {
  position: fixed;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
  z-index: 9999;
  background: rgba(0, 0, 0, 0.48);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 40rpx 32rpx;
  box-sizing: border-box;
}

.win-pet-card {
  width: 100%;
  max-width: 620rpx;
  border-radius: 20rpx;
  padding: 36rpx 28rpx 32rpx;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.win-pet-hero-row {
  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: flex-start;
  gap: 24rpx;
  width: 100%;
  margin-bottom: 12rpx;
  box-sizing: border-box;
}

.win-pet-hero {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: flex-start;
}

.win-pet-img {
  width: 200rpx;
  height: 200rpx;
  margin-bottom: 12rpx;
}

.win-pet-img--dual {
  width: 160rpx;
  height: 160rpx;
  margin-bottom: 0;
}

.win-pet-fallback {
  font-size: 140rpx;
  line-height: 1;
  margin-bottom: 8rpx;
}

.win-pet-fallback--dual {
  font-size: 110rpx;
  margin-bottom: 0;
}

.win-pet-name {
  font-size: 30rpx;
  font-weight: 700;
  margin-bottom: 16rpx;
}

.win-pet-body {
  font-size: 26rpx;
  line-height: 1.55;
  text-align: center;
  white-space: pre-wrap;
  word-break: break-all;
  margin-bottom: 28rpx;
  width: 100%;
}

.win-pet-actions {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  width: 100%;
}

.win-pet-btn {
  border-radius: 999rpx;
  padding: 22rpx 24rpx;
  text-align: center;
  font-size: 28rpx;
  font-weight: 600;
}

.win-pet-btn--ghost {
  border-width: 1rpx;
  border-style: solid;
  background: transparent;
}

.win-pet-btn:active {
  opacity: 0.9;
  transform: scale(0.98);
}
